@test [
    $elements = ("one"; "two"; "three");
    each($el; $elements) [
        $a = "{$el}";
    ];
    
    $elements = ("one"; "two");
    each($el; $elements) [
        $b = "{$el}";
    ];

    $elements = ("one");
    each($el; $elements) [
        $c = "{$el}";
    ];
];