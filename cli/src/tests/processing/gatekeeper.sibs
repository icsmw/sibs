#(a: ./)
    @inspecting::hash("../processing"; "../error") -> (:test("a"));
    test($a: a | b) [
        @print("Task test is done with {$a}");
    ];
#(b: ./)
    @inspecting::hash("../processing"; "../error") -> ();
    test($a: a | b) [
        @print("Task test is done with {$a}");
    ];
