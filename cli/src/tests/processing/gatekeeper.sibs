#(a: ./)
    @hash::inspect(("../processing"; "../error"); (); false) -> (:test("a"));
    test($a: a | b) [
        @print("Task test is done with {$a}");
    ];
#(b: ./)
    @hash::inspect(("../processing"; "../error"); (); false) -> (:test("a"); :test("b"));
    test($a: a | b) [
        @print("Task test is done with {$a}");
    ];
#(c: ./)
    @hash::inspect(("../processing"; "../error"); (); false) -> ();
    test($a: a | b) [
        @print("Task test is done with {$a}");
    ];