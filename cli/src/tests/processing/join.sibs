/// Testing join 
#(join: ../../../../target/debug)
    /// Success exit
    test [
        JOIN (
            `./exit 0 10000 10 100`;
            `./exit 0 10000 10 100`;
            `./exit 1 300 10 100`;
            `./exit 0 10000 10 100`;
        );
    ];
