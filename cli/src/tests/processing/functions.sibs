@test {
    $a = env::os();
    env::is_os($a);
};
@test {
    $a = env::os();
    $b = str::repeat("{env::is_os($a)}", 3);
    if $b == "truetruetrue" {
        true;
    };
};
@test {
    if test::return_bool(true) {
        true;
    };
};
@test {
    if !test::return_bool(false) {
        true;
    };
};