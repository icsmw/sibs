#(a)
    @test($a: a | b) {
        $b = 1234;
        $c = true;
        if $a == "a" {
            "true";
        } else {
            "false";
        };
    };
#(b)
    @test($a: {string}, $b: a | b, $c: {string}) {
        if $a == "a" && $b == "b" && $c == "c" {
            "true";
        } else {
            "false";
        };
    };
#(c)
    @test($a: {string}, $b: a | b, $c: {string}) {
        :b:test("a", "b", "c");
    };
#(d)
    @test($a: {string}, $b: a | b, $c: {string}) {
        :a:test("a");
    };
#(ppm1)
    @test() {
        $strings = ("one", "two", "three");
        $string = $strings[0];
        :a:test($string);
        :a:test($strings[0]);
    };