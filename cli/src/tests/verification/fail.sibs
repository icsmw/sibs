#(a)
    @test($a: a | b) {
        $b = 1234;
        $c = true;
        if $a == "a" && $b == true && $c == 1234 {
            "true";
        } else {
            "false";
        };
    };
#(b)
    @test($a: {string}, $b: a | b, $c: {string}) {
        if $a == 1 && $b == true && $c == false {
            "true";
        } else {
            "false";
        };
    };
#(c)
    @test($a: {string}, $b: a | b, $c: {string}) {
        :b:test(1, true, false);
    };
#(d)
    @test($a: {string}, $b: a | b, $c: {string}) {
        :a:test(true);
    };
#(ppm1)
    @test() {
        $values = (true, false, true);
        $value = $values[0];
        :a:test($value);
    };
#(ppm2)
    @test() {
        $values = (1, 2, 3);
        $value = $values[0];
        :a:test($value);
    };
