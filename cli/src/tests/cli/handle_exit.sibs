/// Testing exit codes
#(exit: ../../../../target/debug)
    /// Success exit
    success [
        @env::is_os(windows) => `exit.exe 0 5000 10 100`;
        @env::is_os(linux) => `./exit 0 5000 10 100`;
    ];
    /// Fail exit
    fail [
        `ls -lsa`;
        `pwd`;
        @env::is_os(windows) => `exit.exe 1 5000 10 100`;
        @env::is_os(linux) => `exit 1 5000 10 100`;
    ];
    /// Custom run
    custom($code: {number})[
        `ls -lsa`;
        `pwd`;
        @env::is_os(windows) => `exit.exe {$code} 5000 10 100`;
        @env::is_os(linux) => `exit {$code} 5000 10 100`;
    ];