each($file; $files) [
    sh(tgz ./release/{$file});
];
each($file; $files) [
    if $file != "." && $file != ".." [
        sh(tgz ./release/{$file});
    ];
];
each($file; $files) [
    if $file != "." && $file != ".." [
        sh(tgz ./release/{$file});
    ];
];
each($file; ls(../output/files)) [
    if $file != "." && $file != ".." [
        sh(tgz ./release/{$file});
    ];
];
each($file; ls(../output/files)) [
    if $file != "." && $file != ".." [
        sh(tgz ./release/{$file});
    ];
];
each($file; ls(../output/files)) [
    if $file != "." && $file != ".." [
        sh(tgz ./release/{$file});
    ];
];
each($file; ls(../output/files)) [
    if $file != "." && $file != ".." [
        `tgz ./release/{$file}`;
    ];
];