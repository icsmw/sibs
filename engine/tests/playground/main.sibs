include from "./nested.sibs";

mod fns {
    fn sum(a: num, b: num) {
        a + b;
    };
    fn diff(a: num, b: num) {
        a - b;
    };
}

/// This is description component_a
component component_a() {
    /// This description is task_a
    task task_a() {
        let a = 5;
        a.fns::sum(a);
    }
    /// This description is task_a
    task task_b() {
        print("component_a:task_b started");
        `ls -lsa`;
        print("component_a:task_b done");

    }
};

/// This is description component_b
component component_b() {
    /// This description is task_a
    task task_a() {
        let a = 5;
        a.fns::sum(a);
    }
};
