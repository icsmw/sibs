mod inner {
    fn sum(a: num, b: num) {
        a + b;
        let d = "fdsfsd";
        let b = `fdfs{func(true, false, 12)}dfs`;
        let t = 'fsdfsdfsdfsd{fds}fsd';
        mod::func();
        func();
        :component:task();
    };
}
fn sum(a: num, b: num) {
    a + b;
};
fn diff(a: num, b: num) {
    a - b;
};