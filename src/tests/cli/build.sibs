/// Test SIBS
#(test: ./tmp)
    /// Building electron application
    try [
        // Will try to execute terminal command
        `ls -lsa`;
        @os(windows);
        @os(linux);
        @os(darwin);
        $a_str = "value_a";
        $b_str = "value_b_{@os(linux)}";
        $c_str = "value_c_{$a_str}-{$b_str}";
        $d_bool = @os(linux);
        IF @os(linux) [
            :self:build("smth"; "prod1");
        ];
    ];
    /// Building electron application
    /// [b]$input_a[/b] {string} [>>] build developing version
    /// [b]$mode[/b]:dev         [>>] build developing version
    /// [b]$mode[/b]:prod        [>>] build production version
    build($input_a: {string}; $mode: dev | prod;) [
        $a_str = "value_a";
        `ls -lsa`;
    ];

